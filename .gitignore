__pycache__/
*.pyc

obsidian_vaults
raw_transcripts
graft
.DS_Store

# per-machine Claude Code overrides (bakes in this checkout's absolute vault paths)
.claude/settings.local.json
acp-claude/.claude/settings.local.json

# rust build output
rust/target/

# local state written by the app
.aibrain/
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm

# local secrets — never commit; see .env.example
.env
.env.local

# dev.sh logs
logs/
