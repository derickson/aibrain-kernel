[package]
name = "aibrain-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Watches Obsidian vaults, keeps Postgres in step, and serves the corpus."

[[bin]]
name = "aibrain-core"
path = "src/main.rs"

[dependencies]
anyhow = "1"
axum = { version = "0.7", features = ["http1", "json", "query", "tokio"] }
blake3 = "1"
dotenvy = "0.15"
notify = "6"
notify-debouncer-full = "0.3"
pulldown-cmark = { version = "0.12", default-features = false, features = ["html"] }
regex = "1"
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls", "json", "charset"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
# No `macros` feature: nothing here uses `sqlx::query!` or `sqlx::migrate!`
# (migrations are applied by `db::migrate` from plain .sql files), and asking
# for it compiles sqlx-macros-core for nothing.
sqlx = { version = "0.8", default-features = false, features = [
  "runtime-tokio", "tls-none", "postgres", "migrate", "chrono", "json",
] }
tokio = { version = "1", features = ["rt-multi-thread", "macros", "signal", "sync", "time", "fs"] }
tokio-stream = { version = "0.1", features = ["sync"] }
tower-http = { version = "0.5", features = ["cors", "trace"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
walkdir = "2"
chrono = { version = "0.4", features = ["serde"] }
